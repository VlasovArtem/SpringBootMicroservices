spring:
  cloud:
    config:
      uri: http://${myConfigServer.name:localhost}:8012
      failFast: true
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 5000
        maxAttempts: 20

